<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Add Inventory Item</title>
</head>
<body>
  <h1>Add New Vehicle</h1>

  <% if (message) { %>
    <p><%= message %></p>
  <% } %>

  <form action="/inventory/add-inventory" method="post">
    <label for="classificationList">Classification:</label>
    <%= classificationList %>

    <label for="invMake">Make:</label>
    <input type="text" id="invMake" name="inv_make" value="<%= inv_make || '' %>" required pattern="^[A-Za-z0-9 ]+$">

    <label for="invModel">Model:</label>
    <input type="text" id="invModel" name="inv_model" value="<%= inv_model || '' %>" required pattern="^[A-Za-z0-9 ]+$">

    <label for="invYear">Year:</label>
    <input type="number" id="invYear" name="inv_year" value="<%= inv_year || '' %>" required min="1900" max="2099">

    <label for="invDescription">Description:</label>
    <input type="text" id="invDescription" name="inv_description" value="<%= inv_description || '' %>" required>

    <label for="invPrice">Price:</label>
    <input type="number" id="invPrice" name="inv_price" value="<%= inv_price || '' %>" required min="0">

    <label for="invMiles">Miles:</label>
    <input type="number" id="invMiles" name="inv_miles" value="<%= inv_miles || '' %>" required min="0">

    <label for="invColor">Color:</label>
    <input type="text" id="invColor" name="inv_color" value="<%= inv_color || '' %>" required>

    <label for="invImage">Image Path:</label>
    <input type="text" id="invImage" name="inv_image" value="<%= inv_image || '/images/no-image.png' %>" required>

    <label for="invThumbnail">Thumbnail Path:</label>
    <input type="text" id="invThumbnail" name="inv_thumbnail" value="<%= inv_thumbnail || '/images/no-image-tn.png' %>" required>

    <button type="submit">Add Vehicle</button>
  </form>
</body>
</html>
