<%- nav %>

<h1><%= title %></h1>

<% if (message && message.length > 0) { %>
  <div class="flash-message"><%= message %></div>
<% } %>

<% if (grid && grid.trim().length > 0) { %>
  <%- grid %>
<% } else { %>
  <p class="notice">Sorry, no vehicles found in this classification.</p>
<% } %>
